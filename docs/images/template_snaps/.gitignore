# Private snapshot patterns are managed manually with demo-safe data.
*plex*.png
*invoke*.png
*nzbget*.png
*pihole*.png
*servarr*.png

# Splash/UI screenshots are not template gallery entries.
close_splash.png
startup_splash.png

# Allow sanitized demo snapshots to be committed.
!*_demo*.png

# Keep gallery metadata and ignore rules tracked.
!manifest.json
!.gitignore
